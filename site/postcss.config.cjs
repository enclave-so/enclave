module.exports = {
  plugins: {
    autoprefixer: {},
    tailwindcss: {},
    // other plugins
  },
}

// TODO: Delete this. Ensure pnpm does not attempt to take the postcss.config.js from the parent folder.